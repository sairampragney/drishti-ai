import Image from "next/image";
import Link from "next/link";
import { ArrowLeft, CheckCircle2, ClipboardCheck } from "lucide-react";
import { AppShell } from "@/components/app-shell";
import { Disclaimer } from "@/components/disclaimer";
import { demoImages } from "@/lib/demo-data";

export default async function Result({ params }: { params: Promise<{ id: string }> }) {
  const { id } = await params;
  const image = demoImages.find((item) => id.includes(item.id)) ?? demoImages[1];
  const imageUrl = process.env.DATASET_ROOT ? `/api/dataset/${image.imagePath.split("/").map(encodeURIComponent).join("/")}` : null;
  return <AppShell><Link href="/scan" className="inline-flex items-center gap-2 text-sm font-bold text-[#486581]"><ArrowLeft size={16} />Back to scan</Link><div className="mt-8 flex flex-wrap items-end justify-between gap-4"><div><p className="eyebrow">Prototype result · {id}</p><h1 className="mt-2 text-4xl font-black">Screening review</h1><p className="mt-2 text-[#627d98]">Aarav Mehta · DR-2401</p></div><span className="flex items-center gap-2 rounded-full bg-[#e7f6f2] px-4 py-2 text-sm font-bold text-[#0f766e]"><CheckCircle2 size={17} />Image detected</span></div><div className="mt-8"><Disclaimer /></div><div className="mt-8 grid gap-6 lg:grid-cols-[.8fr_1.2fr]"><div className="glass grid min-h-[340px] place-items-center rounded-2xl p-6">{imageUrl ? <Image src={imageUrl} alt={`Prototype retinal image ${image.originalFilename}`} width={600} height={450} unoptimized className="max-h-[340px] w-full rounded-xl object-contain" /> : <div className="grid size-64 place-items-center rounded-full border-[18px] border-[#d9f3f0] bg-[radial-gradient(circle_at_42%_40%,#263f5b_0_8%,#4c8c88_9%_25%,#162f48_26%_55%,#0d2338_56%)] shadow-inner"><span className="rounded-full bg-white/80 px-3 py-1 text-xs font-bold text-[#102a43]">mBRSET image</span></div>}</div><section className="glass rounded-2xl p-7"><p className="eyebrow">mBRSET ICDR Dataset Classification</p><h2 className="mt-3 text-3xl font-black">{image.severity}</h2><p className="mt-2 text-sm text-[#627d98]">Dataset label {image.sourceLabel} · Prototype category {image.prototypeCategory}</p><div className="mt-7 rounded-xl bg-[#fff9ed] p-4 text-sm leading-6 text-[#7a4b12]">This is a dataset-derived category for prototype review. It does not indicate the patient&apos;s clinical condition.</div><p className="mt-6 text-sm leading-6 text-[#486581]">The selected image is from the mBRSET prototype image source. Computer vision detected an eye image; it did not produce this dataset classification.</p><button className="button-primary mt-7"><ClipboardCheck size={17} />Save screening</button></section></div></AppShell>;
}
