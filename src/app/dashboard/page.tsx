import Link from "next/link";
import { ArrowUpRight, CalendarDays, Plus, Users } from "lucide-react";
import { AppShell } from "@/components/app-shell";
import { Disclaimer } from "@/components/disclaimer";
import { demoPatients, demoScreenings } from "@/lib/demo-data";
import { DoctorGreeting } from "@/components/doctor-greeting";
export default function Dashboard() { return <AppShell><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="eyebrow">Wednesday, 26 August 2026</p><DoctorGreeting /><p className="mt-2 text-[#627d98]">A quiet workspace for careful review.</p></div><Link href="/scan" className="button-primary"><Plus size={18} />Start new scan</Link></div><div className="mt-8"><Disclaimer /></div><div className="mt-8 grid gap-4 sm:grid-cols-2"><Stat icon={<Users />} label="Synthetic patients" value={demoPatients.length.toString()} /><Stat icon={<CalendarDays />} label="Prototype screenings" value={demoScreenings.length.toString()} /></div><div className="mt-8 grid gap-6 lg:grid-cols-[1.1fr_.9fr]"><section className="glass rounded-2xl p-6"><div className="flex items-center justify-between"><h2 className="text-lg font-extrabold">Recent patients</h2><Link href="/patients" className="text-sm font-bold text-[#0f766e]">View all</Link></div><div className="mt-5 space-y-3">{demoPatients.map((patient) => <Link href="/scan" key={patient.id} className="flex items-center justify-between rounded-xl border border-[#e5edf3] bg-white/60 p-4 hover:border-[#8ad6cc]"><div><p className="font-bold">{patient.name}</p><p className="mt-1 text-xs text-[#627d98]">{patient.patientCode} · {patient.age} years · {patient.gender}</p></div><ArrowUpRight size={18} className="text-[#0f766e]" /></Link>)}</div></section><section className="glass rounded-2xl p-6"><h2 className="text-lg font-extrabold">Latest screening</h2><div className="mt-6"><p className="text-2xl font-black">{demoScreenings[0].datasetImage.severity}</p><p className="mt-2 text-sm leading-6 text-[#627d98]">mBRSET Dataset · {demoScreenings[0].datasetImage.severity}</p><Link href={`/results/${demoScreenings[0].id}`} className="mt-5 inline-flex text-sm font-bold text-[#0f766e]">Open prototype result <ArrowUpRight size={16} className="ml-1" /></Link></div></section></div></AppShell> }
function Stat({ icon, label, value }: { icon: React.ReactNode; label: string; value: string }) { return <div className="glass flex items-center gap-4 rounded-2xl p-5"><div className="grid size-11 place-items-center rounded-xl bg-[#d9f3f0] text-[#0f766e]">{icon}</div><div><p className="text-2xl font-black">{value}</p><p className="text-sm text-[#627d98]">{label}</p></div></div> }