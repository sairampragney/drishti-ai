import { AppShell } from "@/components/app-shell";
import { Disclaimer } from "@/components/disclaimer";
import { demoScreenings } from "@/lib/demo-data";
export default function History() { return <AppShell><p className="eyebrow">Audit trail</p><h1 className="mt-2 text-4xl font-black">Screening history</h1><p className="mt-2 text-[#627d98]">Your saved prototype reviews, scoped to this doctor workspace.</p><div className="mt-6"><Disclaimer /></div><section className="glass mt-8 overflow-hidden rounded-2xl"><div className="divide-y divide-[#e5edf3]">{demoScreenings.map((screening)=><div className="flex flex-wrap items-center justify-between gap-5 bg-white/60 p-6" key={screening.id}><div><p className="font-extrabold">{screening.patientName}</p><p className="mt-1 text-sm text-[#627d98]">{screening.createdAt} · {screening.datasetImage.originalFilename}</p></div><div className="text-left sm:text-right"><p className="text-sm font-bold text-[#0f766e]">{screening.datasetImage.severity}</p><p className="mt-1 text-xs text-[#829ab1]">mBRSET dataset classification</p></div></div>)}</div></section></AppShell> }